namespace Plisky.Versioning {
    public enum VersioningCommand {
        Invalid,
        CreateNewVersion,
        UpdateFiles,
        PassiveOutput,
        BehaviourOutput,
        BehaviourUpdate,
        Override,
        SetDigitValue
    }
}